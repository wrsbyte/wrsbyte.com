---
interface Props {
  tag?: "div" | "section";
  ariaLabel?: string;
  ariaLabelledby?: string;
  className?: string;
}
const { tag = "div", ariaLabel, ariaLabelledby, className } = Astro.props;

const Tag = tag;
---

<Tag
  aria-label={ariaLabel}
  aria-labelledby={ariaLabelledby}
  class:list={[
    "w-full md:max-w-[750px] lg:max-w-[950px] mx-auto px-3",
    className,
  ]}
>
  <slot />
</Tag>
